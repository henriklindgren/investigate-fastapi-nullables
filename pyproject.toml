[project]
name = "investigate-fastapi-nullables"
version = "0.0.1"
description = "Investigating how nullables are handled in the OpenAPI schema generated by FastAPI"

requires-python = ">=3.9"
dependencies = [
    "fastapi[all]==0.95.*",
    "uvicorn",
    "pydantic==1.10.*",
    "pytest",
]
